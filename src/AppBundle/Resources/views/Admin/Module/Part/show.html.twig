{% extends '@App/base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row valign-wrapper">
            <div class="col s6">
                <h2>Modul-Teil</h2>
            </div>
            <div class="col s6 valign right-align">
                <a href="{{ path('admin.module.part.edit', {id: modulePart.id}) }}" class="btn waves-effect waves-light">Bearbeiten</a>
                &nbsp;
                <a href="{{ path('admin.module.index') }}" class="btn waves-effect waves-light blue">Übersicht</a>
            </div>
        </div>
        <div class="row">
            <div class="col s12">
                <table>
                    <tr>
                        <th>Modul:</th>
                        <td>{{ modulePart.module.name }}</td>
                    </tr>
                    <tr>
                        <th>Name:</th>
                        <td>{{ modulePart.name }}</td>
                    </tr>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
