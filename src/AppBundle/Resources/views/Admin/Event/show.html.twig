{% extends '@App/base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row valign-wrapper">
            <div class="col s6">
                <h2>Event</h2>
            </div>
            <div class="col s6 valign right-align">
                <a href="{{ path('admin.event.edit', {id: event.id}) }}" class="btn waves-effect waves-light">Bearbeiten</a>
                &nbsp;
                <a href="{{ path('admin.event.index') }}" class="btn waves-effect waves-light blue">Übersicht</a>
            </div>
        </div>
        <div class="row">
            <div class="col s12">
                <table>
                    <tr>
                        <th>Name:</th>
                        <td>{{ event.name }}</td>
                    </tr>
                    <tr>
                        <th>Start:</th>
                        <td>{{ event.startDate.format('d.m.Y - H:i') }}</td>
                    </tr>
                    <tr>
                        <th>Ende:</th>
                        <td>{{ event.endDate.format('d.m.Y - H:i') }}</td>
                    </tr>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
